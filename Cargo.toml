[package]
name = "github-hosts-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.13.3" , features = ["blocking"] }
scraper = { version = "0.27.0" }
regex = { version = "1.12.3" }
